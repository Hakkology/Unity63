using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AblaColliderController : MonoBehaviour
{
    [SerializeField]
    private CapsuleCollider leftArm;

    [SerializeField]
    private CapsuleCollider rightArm;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
